name: git_client_mobile
description: A Git Client Mobile Application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rounded_floating_app_bar: 0.1.0
  cupertino_icons: ^0.1.2
  date_format: "^1.0.4"
  url_launcher: "^3.0.1"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/AppBarLogo.png
    - assets/backgroundUserImage.png